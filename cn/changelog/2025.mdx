---
title: "更新日志"
description: "本更新日志记录了2025年Codatta的所有更新、修复和新功能。"
---

<div style={{
  backgroundColor: '#ffffff',
  border: '1px solid #e5e7eb',
  borderRadius: '0.75rem',
  padding: '0.875rem 1.25rem',
  marginBottom: '2rem',
  boxShadow: '0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06)'
}}>
  <div style={{ 
    display: 'flex', 
    alignItems: 'center', 
    flexWrap: 'wrap', 
    gap: '1rem',
    marginBottom: '0.1rem'
  }}>
    <ChangelogFilter />
    <MonthFilter />
  </div>
  
  {(() => {
    const ShowResult = () => {
      const [num, setNum] = useState(0);
      useEffect(() => {
        if (typeof document === 'undefined') return;
        const update = () => {
          try {
            const items = document.querySelectorAll('.changelog-item');
            let count = 0;
            items.forEach(item => {
              if (item.style.display !== 'none') count++;
            });
            setNum(count);
          } catch {}
        };
        update();
        const id = setInterval(update, 2000);
        return () => clearInterval(id);
      }, []);
      return (
        <div style={{
          display: 'flex',
          alignItems: 'center',
          padding: '0.5rem 0.875rem',
          backgroundColor: '#f9fafb',
          borderRadius: '0.5rem',
          fontSize: '0.8125rem',
          color: '#6b7280',
          border: '1px solid #e5e7eb',
          display: 'inline-flex',
          fontWeight: '500',
          letterSpacing: '0.01em'
        }}>
          <span style={{
            display: 'inline-block',
            width: '6px',
            height: '6px',
            borderRadius: '50%',
            backgroundColor: '#10b981',
            marginRight: '0.5rem',
            flexShrink: 0
          }}></span>
          <strong style={{color:'#111827', fontWeight:'600', marginRight: '0.25rem'}}>{num}</strong>
          <span>结果{num !== 1 ? 's' : ''}</span>
        </div>
      );
    };
    return <ShowResult />;
  })()}
</div>
<div className="changelog-item" data-type="core-feature" data-month="dec">

## 2025年12月05日
---
**激励机制演变：对长期生态系统管理的奖励锁定**

<br />

<div style={{ marginTop: "1rem" }}></div>

针对高激励任务（如空投或前沿活动）奖励的可选锁定功能。

- **触发：**活动结束后，奖励将存入您的账户余额。
- **锁定：**通过资产页面将奖励锁定在智能合约中。
- **释放：**在锁定期结束后，一键将奖励提取到您的钱包。

这是我们激励机制向细致管理演变的重要一步。它提供了一个在高奖励场景下管理流动性的工具，平衡短期参与与生态系统长期健康。

</div>
<div className="changelog-item" data-type="core-feature" data-month="dec">

## 2025年12月04日
---
**激励机制演变：长期生态系统管理的奖励锁定**

<br />

<div style={{ marginTop: "1rem" }}></div>

针对高激励任务（如空投或前沿活动）奖励的可选锁定功能。

- **触发：**活动结束后，奖励将存入您的账户余额。
- **锁定：**通过资产页面将奖励锁定在智能合约中。
- **释放：**在锁定期结束后，一键将奖励提取到您的钱包。

这是我们激励机制向细致管理演变的重要一步。它提供了一个在高奖励场景下管理流动性的工具，平衡短期参与与生态系统长期健康。

</div>

<div className="changelog-item" data-type="campaign" data-month="nov">

## 2025年11月24日
---
**活动发布：第二季空投**

<br />

<div style={{ marginTop: "1rem" }}></div>

系统性收集物理、金融和多模态领域的学术级数据集，为三个新前沿建立高质量数据管道。

- **行动：**推出第二季空投，总奖池为100万美元（3个月线性解锁）。
- **新前沿：**
  - **高级物理问题：**供领域专家提交当前AI难以回答的高难度物理问题。
  - **加密与股票信息：**为AI提供与加密货币和股票投资决策相关的可靠信息。
  - **现实世界照片：**为AI提供包含注释元数据的现实世界照片。
- **奖励结构：**任务完成获得90%，排行榜排名获得10%。
- **时间：**2025年11月24日09:00 UTC至2025年12月08日09:00 UTC。

</div>

<div className="changelog-item" data-type="core-feature" data-month="nov">

## 2025年11月05日
---
**账户系统升级：去中心化标识符（DID）正式推出**

<br />

<div style={{ marginTop: "1rem" }}></div>

基础升级实现账户与去中心化标识符（DID）的绑定。

- **绑定：**通过下次登录Codatta，自助服务集成链上DID。
- **关联：**绑定的DID作为统一主键，系统性索引所有任务操作、数据贡献和奖励分配。
- **追踪：**实现链上与链下活动的完整追溯和验证，建立不可变的数据来源。

该部署为可验证的数据经济奠定了技术基础。通过为每位贡献者提供一个持久的、独特的数字身份，我们实现了细致的贡献归属、明确的所有权验证以及在协议层面的透明激励分配。

</div>

<div className="changelog-item" data-type="fixes" data-month="nov">

## 2025年11月03日
---
**任务系统调整：加密前沿任务模块退役**

<br />

<div style={{ marginTop: "1rem" }}></div>

整个加密前沿任务模块及所有相关任务（包括提交、验证和赏金猎人）已下线。

- **原因：**该模块已按当前产品路线图完成计划生命周期。退役有助于系统整合和资源重新分配，以支持即将发布的新功能。
- **注意：**用户的历史贡献记录和从该模块获得的奖励保持不变。此更改对其他前沿模块没有影响。

</div>

<div className="changelog-item" data-type="fixes" data-month="oct">

## 2025年10月24日
---
**前沿系统调整：机器人前沿退役**

<br />

<div style={{ marginTop: "1rem" }}></div>

机器人前沿已正式退役。此举仅影响前端入口。所有历史贡献数据保持不变并可访问。

- **原因：**这一决定是我们持续产品整合的一部分，将资源和用户关注点集中在我们最高优先级的活跃前沿。机器人前沿已完成计划的探索阶段，其退役将有助于我们整合工作。
- **注意：**用户在机器人前沿的历史贡献和奖励会被保留，并可以在其贡献历史中查看。没有其他前沿或平台功能受到影响。

</div>

<div className="changelog-item" data-type="optimization" data-month="oct">

## 2025年10月23日
---
**反垃圾增强：实施每日提交限制**

<br />

<div style={{ marginTop: "1rem" }}></div>

新平台安全政策，防止任务灌水。

- **实施方式：**为每个任务强制配置的每日提交上限。
- **原因：**保护系统资源，确保所有贡献者公平参与，维持任务生态系统的长期健康和公平性。

</div>

<div className="changelog-item" data-type="fixes" data-month="oct">

## 2025年10月20日
---
**前沿系统调整：加密前沿退役**

<br />

<div style={{ marginTop: "1rem" }}></div>

加密前沿已正式退役。此举仅影响前端入口。所有历史贡献数据保持不变并可访问。

- **原因：**这一决定是我们持续产品整合的一部分，将资源和用户关注点集中在我们最高优先级的活跃前沿。加密前沿已完成计划的探索阶段，其退役将有助于我们整合工作。
- **注意：**用户在加密前沿的历史贡献和奖励会被保留，并可以在其贡献历史中查看。没有其他前沿或平台功能受到影响。

</div>

<div className="changelog-item" data-type="optimization" data-month="oct">

## 2025年10月15日
---
**系统治理：平台黑名单控制实施**

<br />

<div style={{ marginTop: "1rem" }}></div>

在平台上实施了新的黑名单控制功能。

- **实施方式：**指定管理员现在可以应用黑名单规则以限制恶意账户。
- **原因：**增强平台安全性，保护贡献者利益，维护生态系统的公平性。

</div>

<div className="changelog-item" data-type="campaign" data-month="oct">

## 2025年10月13日
---
**活动发布：第一季空投**

<br />

<div style={{ marginTop: "1rem" }}></div>

通过高奖励机制和反垃圾技术系统性收集高质量、结构化数据，确保有效性和学术价值，奖励长期贡献者并通过五个新前沿模块多样化平台数据生态。

- **行动：**推出第一季空投，总奖池为250万美元和积分奖励。
- **新前沿：**
  - **模型比较：**比较不同AI模型的性能指标和商业结果，以识别最佳或混合解决方案。
  - **识别LLM错误：**识别LLM输出中的推理、事实和逻辑错误，以支持模型优化。
  - **修正LLM错误：**收集纠正性数据以帮助LLM从错误中学习，加强自我纠正和推理能力。
  - **食品科学：**收集食品科学和营养功能方面的研究数据，以推动该领域的创新。
  - **生活记录画布：**跟踪和记录个人行为和健康相关数据，以支持行为及相关研究。
- **奖励结构：**根据提交评级进行排名，活动结束后分配奖励。恶意提交将受到惩罚以确保数据质量。
- **时间：**2025年10月13日09:00 UTC - 2025年10月27日09:00 UTC。

</div>

<div className="changelog-item" data-type="core-feature" data-month="oct">

## 2025年10月10日
---
**功能发布：前沿特定奖励活动**

<br />

<div style={{ marginTop: "1rem" }}></div>

前沿现可配置的奖励活动，提供额外高价值激励。

- **实施方式：**选定的前沿任务提供额外的$XNY或USDT奖励，与基础积分无关。活动参数（奖励金额、持续时间、目标）根据每个前沿设置，并在各自的主页上显示。
- **原因：**通过有针对性的激励措施，推动在高价值数据任务中的更深入参与，提高数据质量和生态系统参与度。

</div>

<div className="changelog-item" data-type="campaign" data-month="sep">

## 2025年9月26日
---
**活动发布：Codatta助推器活动第三季第四周**

<br />

<div style={{ marginTop: "1rem" }}></div>

通过可信的多领域数据收集，加速AI开发，促进社区驱动的去中心化AI知识构建。

- **访问：**通过Binance Wallet Booster标签或主页横幅进入（需Alpha积分≥61）。
- **奖励：**总奖池为5000万$XNY（带锁定期），在任务完成和验证后分配。
- **时间：**2025年9月26日07:00 UTC - 2025年10月3日07:00 UTC。

</div>

<div className="changelog-item" data-type="campaign" data-month="sep">

## 2025年9月12日
---
**活动发布：Codatta助推器活动第三季第三周**

<br />

<div style={{ marginTop: "1rem" }}></div>

收集生命、机器人、加密、模型比较和指纹验证跨领域数据，用于AI培训和验证。

- **访问：**通过Binance Wallet Booster标签或主页横幅进入（需Alpha积分≥61）。
- **奖励：**总奖池为5000万$XNY（带锁定期），在任务完成和验证后分配。
- **时间：**2025年9月12日07:00 UTC - 2025年9月19日07:00 UTC。

</div>

<div className="changelog-item" data-type="campaign" data-month="sep">

## 2025年9月05日
---
**活动发布：Codatta助推器活动第三季第二周**

<br />

<div style={{ marginTop: "1rem" }}></div>

收集和注释生命、机器人和加密领域的跨域数据，用于AI培训和验证。

- **访问：**通过Binance Wallet Booster标签或主页横幅进入（需Alpha积分≥61）。
- **奖励：**总奖池为5000万$XNY（带锁定期），在任务完成和验证后分配。
- **时间：**2025年9月5日07:00 UTC - 2025年9月12日07:00 UTC。

</div>

<div className="changelog-item" data-type="core-feature" data-month="aug">

## 2025年8月28日
---
**功能发布：用户数据档案**

<br />

<div style={{ marginTop: "1rem" }}></div>

在用户信息模块中引入新的数据档案功能。

- **实施方式：**通过用户信息 > 数据档案查看您的总提交、获得的奖励和贡献统计信息，以可视化面板展示。
- **原因：**提供对您贡献的透明度，通过可见的进度跟踪增强参与感，支持长期参与。

</div>

<div className="changelog-item" data-type="campaign" data-month="aug">

## 2025年8月22日
---
**活动发布：Codatta助推器活动第三季第一周**

<br />

<div style={{ marginTop: "1rem" }}></div>

为去中心化AI培训建立生命、机器人和加密领域的跨域注释数据基础。

- **访问：**通过Binance Wallet Booster标签或主页横幅进入（需Alpha积分≥61）。
- **奖励：**总奖池为5000万$XNY（带锁定期），在任务完成和验证后分配。
- **时间：**2025年8月22日07:00 UTC - 2025年8月29日07:00 UTC。

</div>

<div className="changelog-item" data-type="campaign" data-month="aug">

## 2025年8月15日
---
**活动发布：Codatta助推器活动第二季第四周**

<br />

<div style={{ marginTop: "1rem" }}></div>

系统性注释和扩展食品AI、机器人交互和CEX链上数据的特定领域数据集，以推进去中心化AI培训。

- **访问：**通过Binance Wallet Booster标签或主页横幅进入（需Alpha积分≥61）。
- **奖励：**总奖池为5000万$XNY（带锁定期），在任务完成和验证后分配。
- **时间：**2025年8月15日07:00 UTC - 2025年8月22日07:00 UTC。

</div>

<div className="changelog-item" data-type="campaign" data-month="aug">

## 2025年8月08日
---
**活动发布：Codatta助推器活动第二季第三周**

<br />

<div style={{ marginTop: "1rem" }}></div>

系统性收集四个关键领域的注释数据：项目学习、食品AI判断、机器人注释和CEX数据扩展。

- **访问：**通过Binance Wallet Booster标签或主页横幅进入（需Alpha积分≥61）。
- **奖励：**总奖池为5000万$XNY（带锁定期），在任务完成和验证后分配。
- **时间：**2025年8月8日07:00 UTC - 2025年8月15日07:00 UTC。

</div>

<div className="changelog-item" data-type="campaign" data-month="aug">

## 2025年8月01日
---
**活动发布：Codatta助推器活动第二季第二周**

<br />

<div style={{ marginTop: "1rem" }}></div>

收集三个核心领域的注释数据：AI食品模型比较、机器人交互注释和CEX热钱包数据标记。

- **访问：**通过Binance Wallet Booster标签或主页横幅进入（需Alpha积分≥61）。
- **奖励：**总奖池为5000万$XNY（带锁定期），在任务完成和验证后分配。
- **时间：**2025年8月1日07:00 UTC - 2025年8月8日07:00 UTC。

</div>

<div className="changelog-item" data-type="campaign" data-month="jul">

## 2025年7月24日
---
**活动发布：Codatta助推器活动第二季第一周**

<br />

<div style={{ marginTop: "1rem" }}></div>

通过注释的真实世界交互数据来验证AI食品分析和增强机器人培训。

- **访问：**通过Binance Wallet Booster标签或主页横幅进入（需Alpha积分≥61）。
- **奖励：**总奖池为5000万$XNY（带锁定期），在任务完成和验证后分配。
- **时间：**2025年7月24日07:00 UTC - 2025年7月31日07:00 UTC。

</div>

<div className="changelog-item" data-type="campaign" data-month="jul">

## 2025年7月16日
---
**活动发布：Codatta助推器活动第一季第四周**

<br />

<div style={{ marginTop: "1rem" }}></div>

通过收集丰富的注释，深化AI对食品的理解，超越简单标签，包括重量、烹饪方法和卡路里含量。

- **访问：**通过Binance Wallet Booster标签或主页横幅进入（需Alpha积分≥61）。
- **奖励：**总奖池为5000万$XNY，活动结束后分配。
- **时间：**2025年7月16日13:00 UTC - 2025年7月23日13:00 UTC。

</div>

<div className="changelog-item" data-type="campaign" data-month="jul">

## 2025年7月09日
---
**活动发布：Codatta助推器活动第一季第三周**

<br />

<div style={{ marginTop: "1rem" }}></div>

通过收集准备好的食物注释图像，教会AI对人类饮食习惯的细致理解，关注文化和情境的相关性，而不仅仅是简单标签。

- **访问：**通过Binance Wallet Booster标签或主页横幅进入（需Alpha积分≥61）。
- **奖励：**总奖池为5000万$XNY，活动结束后分配。
- **时间：**2025年7月9日13:00 UTC - 2025年7月16日13:00 UTC。

</div>

<div className="changelog-item" data-type="optimization" data-month="jul">

## 2025年7月07日
---
**功能优化：任务系统恢复**

<br />

<div style={{ marginTop: "1rem" }}></div>

任务系统已完全恢复并重新开放。

- **实施方式：**于2025年6月30日暂时下线，现在已全面恢复。
- **原因：**通过架构升级提高系统稳定性和用户体验。

</div>

<div className="changelog-item" data-type="campaign" data-month="jul">

## 2025年7月02日
---
**活动发布：Codatta助推器活动第一季第二周**

<br />

<div style={{ marginTop: "1rem" }}></div>

通过细致的图像标记，扩大AI对全球食品文化和偏好的理解，涵盖素食、非素食和混合类别。

- **访问：**通过Binance Wallet Booster标签或主页横幅进入（需Alpha积分≥61）。
- **奖励：**总奖池为5000万$XNY，活动结束后分配。
- **时间：**2025年7月2日13:00 UTC - 2025年7月9日13:00 UTC。

</div>

<div className="changelog-item" data-type="campaign" data-month="jun">

## 2025年6月25日
---
**活动发布：Codatta助推器活动第一季第一周**

<br />

<div style={{ marginTop: "1rem" }}></div>

通过收集注释食品数据，进行社区知识竞赛，启动首季，同时通过多层次激励机制奖励参与。

- **访问：**通过Binance Wallet Booster标签或主页横幅进入（需Alpha积分≥61）。
- **奖励：**总奖池为5000万$XNY，活动结束后分配。
- **时间：**2025年6月25日13:00 UTC - 2025年7月2日13:00 UTC。

</div>

{/* 组件定义 - 移动至文件末尾以清理代码组织 */}
export const ChangelogFilter = () => {
  const [activeFilter, setActiveFilter] = useState('all');
  const [isOpen, setIsOpen] = useState(false);
  const [isMenuHovered, setIsMenuHovered] = useState(false);

  useEffect(() => {
    window.activeTypeFilter = activeFilter;
  }, [activeFilter]);

  useEffect(() => {
    const items = document.querySelectorAll('.changelog-item');
    const activeMonth = window.activeMonthFilter || 'all';
    
    items.forEach((item) => {
      const itemType = item.getAttribute('data-type');
      const itemMonth = item.getAttribute('data-month');
      
      const typeMatch = activeFilter === 'all' || itemType === activeFilter;
      const monthMatch = activeMonth === 'all' || itemMonth === activeMonth;
      
      item.style.display = typeMatch && monthMatch ? '' : 'none';
    });
  }, [activeFilter]);

  const filterTypes = [
    { id: 'all', label: '全部', color: '#6b7280', count: 24 },
    { id: 'core-feature', label: '核心功能发布', color: '#16A34A', count: 4 },
    { id: 'optimization', label: '调整与优化', color: '#F59E0B', count: 3 },
    { id: 'fixes', label: '修复与功能退役', color: '#EF4444', count: 3 },
    { id: 'campaign', label: '活动发布', color: '#A855F7', count: 14 }
  ];

  const activeType = filterTypes.find(type => type.id === activeFilter) || filterTypes[0];

  return (
    <div style={{ marginBottom: '2rem', position: 'relative' }}>
      <div
        onClick={() => setIsOpen(!isOpen)}
        style={{
          display: 'inline-flex',
          alignItems: 'center',
          gap: '0.5rem',
          padding: '0.625rem 1rem',
          border: '2px solid #e5e7eb',
          borderRadius: '0.5rem',
          background: 'white',
          cursor: 'pointer',
          fontSize: '0.875rem',
          fontWeight: '500',
          boxShadow: isOpen ? `0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)` : '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
          transition: 'all 0.2s ease',
          userSelect: 'none',
          minWidth: '200px'
        }}
      >
        <span
          style={{
            width: '12px',
            height: '12px',
            borderRadius: '50%',
            backgroundColor: activeType.color,
            display: 'inline-block',
            flexShrink: 0
          }}
        />
        <span style={{ flex: 1 }}>
          {activeType.label} ({activeType.count})
        </span>
        <span
          style={{
            display: 'inline-block',
            width: '0',
            height: '0',
            borderLeft: '5px solid transparent',
            borderRight: '5px solid transparent',
            borderTop: '6px solid #6b7280',
            transform: isOpen ? 'rotate(180deg)' : 'rotate(0deg)',
            transition: 'transform 0.2s ease',
            flexShrink: 0
          }}
        />
      </div>
      
      {isOpen && (
        <div
          onMouseEnter={() => setIsMenuHovered(true)}
          onMouseLeave={() => setIsMenuHovered(false)}
          style={{
            position: 'absolute',
            top: '100%',
            left: 0,
            marginTop: '0.5rem',
            backgroundColor: 'white',
            border: `2px solid ${isMenuHovered ? '#9ca3af' : '#e5e7eb'}`,
            borderRadius: '0.5rem',
            boxShadow: isMenuHovered 
              ? '0 10px 15px -3px rgba(0, 0, 0, 0.15), 0 4px 6px -2px rgba(0, 0, 0, 0.1)' 
              : '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
            zIndex: 1000,
            minWidth: '280px',
            overflow: 'hidden',
            transition: 'border-color 0.2s ease, box-shadow 0.2s ease'
          }}
        >
          {filterTypes.map((type) => (
            <div
              key={type.id}
              onClick={() => {
                setActiveFilter(type.id);
                setIsOpen(false);
              }}
              style={{
                padding: '0.75rem 1rem',
                cursor: 'pointer',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'space-between',
                borderBottom: type.id !== filterTypes[filterTypes.length - 1].id ? '1px solid #f3f4f6' : 'none',
                backgroundColor: activeFilter === type.id ? '#f9fafb' : 'white',
                transition: 'background-color 0.15s ease'
              }}
              onMouseEnter={(e) => {
                if (activeFilter !== type.id) {
                  e.currentTarget.style.backgroundColor = '#f3f4f6';
                }
              }}
              onMouseLeave={(e) => {
                if (activeFilter !== type.id) {
                  e.currentTarget.style.backgroundColor = 'white';
                }
              }}
            >
              <span style={{ flex: 1, fontSize: '0.875rem', fontWeight: activeFilter === type.id ? '600' : '400' }}>
                {type.label}
              </span>
              <span style={{ 
                fontSize: '0.75rem', 
                color: '#6b7280',
                fontWeight: '500'
              }}>
                {type.count}
              </span>
            </div>
          ))}
        </div>
      )}
      
      {isOpen && (
        <div
          onClick={() => setIsOpen(false)}
          style={{
            position: 'fixed',
            top: 0,
            left: 0,
            right: 0,
            bottom: 0,
            zIndex: 999,
            backgroundColor: 'transparent'
          }}
        />
      )}
    </div>
  );
};

export const MonthFilter = () => {
  const [activeMonth, setActiveMonth] = useState('all');
  const [isOpen, setIsOpen] = useState(false);
  const [isMenuHovered, setIsMenuHovered] = useState(false);

  useEffect(() => {
    window.activeMonthFilter = activeMonth;
    const event = new Event('monthFilterChange');
    window.dispatchEvent(event);
  }, [activeMonth]);

  useEffect(() => {
    const handleMonthChange = () => {
      const items = document.querySelectorAll('.changelog-item');
      const activeType = window.activeTypeFilter || 'all';
      
      items.forEach((item) => {
        const itemType = item.getAttribute('data-type');
        const itemMonth = item.getAttribute('data-month');
        
        const typeMatch = activeType === 'all' || itemType === activeType;
        const monthMatch = activeMonth === 'all' || itemMonth === activeMonth;
        
        item.style.display = typeMatch && monthMatch ? '' : 'none';
      });
    };

    window.addEventListener('monthFilterChange', handleMonthChange);
    handleMonthChange();
    
    return () => {
      window.removeEventListener('monthFilterChange', handleMonthChange);
    };
  }, [activeMonth]);

  const months = [
    { id: 'all', label: '所有月份', count: 24 },
    { id: 'dec', label: '十二月', count: 1 },
    { id: 'nov', label: '十一月', count: 3 },
    { id: 'oct', label: '十月', count: 6 },
    { id: 'sep', label: '九月', count: 3 },
    { id: 'aug', label: '八月', count: 5 },
    { id: 'jul', label: '七月', count: 5 },
    { id: 'jun', label: '六月', count: 1 }
  ];

  const activeMonthData = months.find(month => month.id === activeMonth) || months[0];

  return (
    <div style={{ marginBottom: '2rem', position: 'relative', marginLeft: '1rem' }}>
      <div
        onClick={() => setIsOpen(!isOpen)}
        style={{
          display: 'inline-flex',
          alignItems: 'center',
          gap: '0.5rem',
          padding: '0.625rem 1rem',
          border: '2px solid #e5e7eb',
          borderRadius: '0.5rem',
          background: 'white',
          cursor: 'pointer',
          fontSize: '0.875rem',
          fontWeight: '500',
          boxShadow: isOpen ? `0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)` : '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
          transition: 'all 0.2s ease',
          userSelect: 'none',
          minWidth: '200px'
        }}
      >
        <span style={{ flex: 1 }}>
          {activeMonthData.label} ({activeMonthData.count})
        </span>
        <span
          style={{
            display: 'inline-block',
            width: '0',
            height: '0',
            borderLeft: '5px solid transparent',
            borderRight: '5px solid transparent',
            borderTop: '6px solid #6b7280',
            transform: isOpen ? 'rotate(180deg)' : 'rotate(0deg)',
            transition: 'transform 0.2s ease',
            flexShrink: 0
          }}
        />
      </div>
      
      {isOpen && (
        <div
          onMouseEnter={() => setIsMenuHovered(true)}
          onMouseLeave={() => setIsMenuHovered(false)}
          style={{
            position: 'absolute',
            top: '100%',
            left: 0,
            marginTop: '0.5rem',
            backgroundColor: 'white',
            border: `2px solid ${isMenuHovered ? '#9ca3af' : '#e5e7eb'}`,
            borderRadius: '0.5rem',
            boxShadow: isMenuHovered 
              ? '0 10px 15px -3px rgba(0, 0, 0, 0.15), 0 4px 6px -2px rgba(0, 0, 0, 0.1)' 
              : '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
            zIndex: 1000,
            minWidth: '280px',
            overflow: 'hidden',
            transition: 'border-color 0.2s ease, box-shadow 0.2s ease'
          }}
        >
          {months.map((month) => (
            <div
              key={month.id}
              onClick={() => {
                setActiveMonth(month.id);
                setIsOpen(false);
              }}
              style={{
                padding: '0.75rem 1rem',
                cursor: 'pointer',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'space-between',
                borderBottom: month.id !== months[months.length - 1].id ? '1px solid #f3f4f6' : 'none',
                backgroundColor: activeMonth === month.id ? '#f9fafb' : 'white',
                transition: 'background-color 0.15s ease'
              }}
              onMouseEnter={(e) => {
                if (activeMonth !== month.id) {
                  e.currentTarget.style.backgroundColor = '#f3f4f6';
                }
              }}
              onMouseLeave={(e) => {
                if (activeMonth !== month.id) {
                  e.currentTarget.style.backgroundColor = 'white';
                }
              }}
            >
              <span style={{ flex: 1, fontSize: '0.875rem', fontWeight: activeMonth === month.id ? '600' : '400' }}>
                {month.label}
              </span>
              <span style={{ 
                fontSize: '0.75rem', 
                color: '#6b7280',
                fontWeight: '500'
              }}>
                {month.count}
              </span>
            </div>
          ))}
        </div>
      )}
      
      {isOpen && (
        <div
          onClick={() => setIsOpen(false)}
          style={{
            position: 'fixed',
            top: 0,
            left: 0,
            right: 0,
            bottom: 0,
            zIndex: 999,
            backgroundColor: 'transparent'
          }}
        />
      )}
    </div>
  );
};